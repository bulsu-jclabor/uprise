import 'package:flutter/material.dart';

class StudentOrganizationsScreen extends StatelessWidget {
  const StudentOrganizationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Organizations')),
      body: const Center(child: Text('Organizations Screen - Coming Soon')),
    );
  }
}